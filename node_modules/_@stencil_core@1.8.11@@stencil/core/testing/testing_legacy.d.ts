import { Compiler, Config, DevServer, ITestingLegacy } from '@stencil/core/internal';
import * as puppeteer from 'puppeteer';
export declare class TestingLegacy implements ITestingLegacy {
    isValid: boolean;
    compiler: Compiler;
    config: Config;
    devServer: DevServer;
    puppeteerBrowser: puppeteer.Browser;
    constructor(config: Config);
    runTests(): Promise<boolean>;
    destroy(): Promise<void>;
}

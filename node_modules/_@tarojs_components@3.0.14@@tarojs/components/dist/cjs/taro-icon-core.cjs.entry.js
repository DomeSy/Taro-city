'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const core = require('./core-06196d96.js');
const index$1 = require('./index-00392f71.js');

const Icon = class {
    constructor(hostRef) {
        core.registerInstance(this, hostRef);
        this.size = '23';
    }
    render() {
        const { type, size, color } = this;
        const iconType = type === null || type === void 0 ? void 0 : type.replace(/_/g, '-');
        const cls = index$1.classNames({
            [`weui-icon-${iconType}`]: true
        });
        const style = {
            'font-size': `${size}px`,
            color
        };
        return (core.h(core.Host, { class: cls, style: style }));
    }
};

exports.taro_icon_core = Icon;

import { r as registerInstance, h } from './core-de9c9c2d.js';

const Label = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
    }
    render() {
        return (h("label", { htmlFor: this.for }, h("slot", null)));
    }
};

export { Label as taro_label_core };

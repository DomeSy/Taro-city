import { r as registerInstance, h, H as Host } from './core-de9c9c2d.js';

const MoveableView = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
    }
    componentDidLoad() {
        console.error('H5 暂不支持 MoveableView 组件！');
    }
    render() {
        return (h(Host, null));
    }
};

export { MoveableView as taro_moveable_view_core };

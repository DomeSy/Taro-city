import { r as registerInstance, c as createEvent, h } from './core-de9c9c2d.js';

const WebView = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.onLoad = createEvent(this, "load", 7);
        this.onError = createEvent(this, "error", 7);
    }
    render() {
        const { src, onLoad, onError } = this;
        return (h("iframe", { class: 'taro-webview', onLoad: (e) => {
                e.stopPropagation();
                onLoad.emit({ src });
            }, onError: (e) => {
                e.stopPropagation();
                onError.emit({ src });
            }, src: src }));
    }
    static get style() { return "iframe{border:none}.taro-webview{position:fixed;top:0;bottom:0;width:100%;height:100%;z-index:999}"; }
};

export { WebView as taro_web_view_core };

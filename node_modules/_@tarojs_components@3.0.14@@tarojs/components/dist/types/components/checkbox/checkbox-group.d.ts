import { ComponentInterface, EventEmitter } from '../../stencil.core';
export declare class CheckboxGroup implements ComponentInterface {
    private uniqueName;
    private value;
    name: any;
    el: HTMLElement;
    onChange: EventEmitter;
    function(e: CustomEvent<{
        value: string;
    }>): void;
    componentDidLoad(): void;
    getValues(childList: NodeListOf<HTMLTaroCheckboxCoreElement>): string[];
    render(): any;
}
